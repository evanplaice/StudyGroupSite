import React from 'react'
import Logo from '../../assets/bc-logo-NEW.png';

const LogoIcon = ({ height, width }) => (
  <img height={height} width={width} src={Logo} alt="logo" />
);

export default LogoIcon;

